import styled from 'styled-components';

export const MenuPrincipal = styled.div`
  & {
    width: 100%;
  }
  & > nav {
    padding: 8px 40px;
  }
  #root {
    width: 100%;
    align-items: unset;
  }

  & > nav > button {
    width: unset;
  }
`;
